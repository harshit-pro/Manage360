import java.sql.*;

public class CheckDB {
    public static void main(String[] args) throws Exception {
        String url = "jdbc:postgresql://localhost:5432/manage360"; // Guessing the DB name and port
        // We will try standard postgresql ports and names, or parse application.properties
    }
}
